<%= form_for(@survey_instance) do |f| %>
  <% if @survey_instance.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@survey_instance.errors.count, "error") %> prohibited this survey_instance from being saved:</h2>

      <ul>
      <% @survey_instance.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :patient_id %><br>
    <%= f.text_field :patient_id %>
  </div>
  <div class="field">
    <%= f.label :survey_id %><br>
    <%= f.text_field :survey_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
